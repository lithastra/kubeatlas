# Helm Chart

Placeholder for the KubeAtlas Helm Chart. The chart, its templates,
its `values.schema.json` security gates, and the `helm-unittest`
suite are introduced in Phase 1 W10.

Until then this directory exists only to reserve the layout and keep
the repository tree stable.
